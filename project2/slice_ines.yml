experiment_name: project2
yml_output_folder: experiments/test1
run_cfg:
  # preproc/oversampling/enabled: [true, false]
  preproc/rmf/pca/model/n_components: [10, 50, 100, 200, 500]
  models/svc/C: [1, 5]
  models/svc/gamma: ['scale', 'auto']
  models/svc/kernel: ['linear', 'poly', 'rbf', 'sigmoid', 'precomputed']

  # Also test gamma, SVM kernels
